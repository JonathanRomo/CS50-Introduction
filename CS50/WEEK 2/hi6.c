#include <stdio.h>
#include <cs50.h>

int main (void)
{
    string s = "HI!";
    printf("%i%i%i\n", s[0], s[1], s[2], s[3]);
    // last one comes from bits. The null that tells it to stop the string
}