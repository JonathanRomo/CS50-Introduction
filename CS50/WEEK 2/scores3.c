#include <studio.h>

int main (void)
{
    int score1 = 72;
    int score2 = 73;
    int score3 = 74;

    printf("Average: %f/n", (score1 + score2 + score3) / (float) 3);
//declares the division is a float
}